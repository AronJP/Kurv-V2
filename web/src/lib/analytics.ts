/**
 * Single analytics system — placeholder.
 * Will track events (e.g. via Supabase or external service).
 */

export function track(_event: string, _data?: Record<string, unknown>) {
  // TODO: implement
}
